package exemplos;

public class Comparar {

	public static void main(String[] args) {
			int num1 = 10, num2 = 15;
			String resultado;
/*  
			if (num1 > num2) {
				System.out.println("Num1 é maior.");
			} else if (num2 > num1) {
				System.out.println("Num 2 é maior");
			} else {
				System.out.println("Iguais");
			}
*/
			//TERNÁRIO
			//System.out.println(num1 > num2 ? "Num 1 maior" : num2 > num1 ? "Num 2 maior" : "Iguais");
			resultado = (num1 > num2 ? "Num 1 maior" : num2 > num1 ? "Num 2 maior" : "Iguais");
	}
		
		
	
}
